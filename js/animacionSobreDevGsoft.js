var animation = lottie.loadAnimation({
  container: document.getElementById("lottie-animation"), // El contenedor donde se cargará la animación
  renderer: "svg", // Usamos SVG para una renderización más eficiente
  loop: true, // La animación se repetirá de forma infinita
  autoplay: true, // Inicia automáticamente cuando se carga
  path: "recursos/Animation - 1736463928557.json", // Ruta al archivo .json de la animación
});
